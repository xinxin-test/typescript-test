/**
 * @param {number} x
 * @return {boolean}
 */

const num = 121;
var isPalindrome: (x: number) => boolean = function(x: number) {
  return false;
};

const p = isPalindrome(num);

export default p;
